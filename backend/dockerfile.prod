FROM node:16-alpine3.12 AS builder
ENV repository=application
COPY --from=codepuller /${repository} /${repository}
WORKDIR /${repository}/backend/
RUN npm install
EXPOSE 5000
CMD ["npm", "run", "start"]
