apiVersion: v1
kind: Service
metadata:
  name: monaco-editor
  namespace: default
spec:
  ports:
    - port: 
      protocol: TCP
      targetPort: 
  selector:
    name: monaco-editor-pod-label
  type: ClusterIP 
