apiVersion: v1
kind: Service
metadata:
  name: orientdb
  namespace: default
spec:
  ports:
    - port: 
      protocol: TCP
      targetPort: 
  selector:
    name: orientdb-pod-label
  type: ClusterIP 
