apiVersion: v1
kind: Service
metadata:
  name: cluster-seed-service
  namespace: default
spec:
  ports:
    - port: 
      protocol: TCP
      targetPort: 
  selector:
    name: cluster-seed-pod-label
  type: ClusterIP 
