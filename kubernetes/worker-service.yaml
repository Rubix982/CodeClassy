apiVersion: v1
kind: Service
metadata:
  name: worker
  namespace: default
spec:
  ports:
    - port: 
      protocol: TCP
      targetPort: 
  selector:
    name: worker-pod-label
  type: ClusterIP 
